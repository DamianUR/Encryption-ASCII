# Encryption-ASCII
This Python script encrypts and decrypts text messages using ASCII values and base conversion. It prompts the user to choose an action, a numeric base (2–36), and a message. For encryption, each character is converted to its ASCII code then represented in the chosen base. Decryption reverses this to recover the original text (visa versa).
